// Bundle entry point. Nothing here yet — the reader, the decoder and the panel
// land in later steps. It exists so the build has something to assemble and the
// gate has something to check.
export {};
