{
  "name": "margometer",
  "version": "0.8.1",
  "description": "Damage meter for Margonem — live fight statistics",
  "homepage": "https://github.com/KamilGrocholski/margometer",
  "author": "Kamil Grocholski",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KamilGrocholski/margometer.git"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "build:dev": "bun run build.ts --dev",
    "preview": "bun run tools/preview-server.ts",
    "preview:site": "bun run tools/preview-site.ts",
    "screenshots": "bun run tools/panel-screenshots.ts",
    "cost": "bun run tools/payload-cost.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.userscript.json",
    "check": "bun run typecheck && bun test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
