{
    "name": "margometer",
    "private": true,
    "description": "The browser suite only. Deno builds and checks this repository; npm carries one dependency, and tests/e2e is the whole of what it is for.",
    "scripts": {
        "e2e": "playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.0",
        "@types/node": "^26.4.1"
    }
}
